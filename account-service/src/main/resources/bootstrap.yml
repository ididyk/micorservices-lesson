eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
      fetch-registry: true
      registerWithEureka: true
spring:
  application:
    name: accounts-ui
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: config
      failFast: true

